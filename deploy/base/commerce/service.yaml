apiVersion: v1
kind: Service
metadata:
  name: commerce
  annotations:
    # See builder/service.yaml for why this annotation is here.
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: commerce
  ports:
    - name: http
      port: 80
      targetPort: http
