apiVersion: v1
kind: ServiceAccount
metadata:
  name: builder
# On GKE this gets the iam.gke.io/gcp-service-account annotation for Workload
# Identity (replacing the Cloud Run --service-account flag). Locally it is a
# plain SA so the manifests stay identical across overlays.
