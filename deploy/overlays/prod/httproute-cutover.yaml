apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: builder-cutover
spec:
  parentRefs:
    - name: palladium
      sectionName: api-cutover
  rules:
    - backendRefs:
        - name: builder
          port: 80