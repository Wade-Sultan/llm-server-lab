# Secrets & Environment
.env
.env.*
*.env
!.env.example
.gcloud/
!.env.template

# Local database snapshots pulled from Cloud SQL. These contain production
# data — including user rows, until seed-local-db.sh scrubs them on restore —
# and must never reach the repository.
.local-seed/
*.dump
*.sql.gz

# Supabase CLI (local dev)
supabase/.temp/
supabase/.branches/
supabase/.keys/

# Python / Backend
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.cache/

# Node / Frontend
node_modules/
dist/
dist-ssr/
*.local

# IDE & OS
.vscode
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

#Testing
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Docker
docker-compose.override.yml

# Next
.next
next-env.d.ts
dist

# Local operator runbook — deliberately untracked. Contains the step-by-step
# Grafana Cloud / GCP setup that only the person holding those consoles needs.
# Code comments reference it by path (cloudbuild.yaml, scripts/logs.sh,
# backend/app/core/metrics.py, scripts/k6/chat.js); those are pointers for
# whoever has the file, not a build dependency.
deploy/observability.md
